export interface IContentsBoxProps {
  friend?: {
    title: string;
    id: string;
    introduce: string;
    idx: number;
  };
  room?: {
    title: string;
  };
  setting?: {
    title: string;
  };
}
